# Mac_changer
Here’s a simple Linux programmme that can change the system’s MAC address. This script uses the subprocess module to run system commands like ifconfig or ip. the menu‑driven MAC manager with logging and a “view log history” option so you can check past changes directly from the script,  Real-Time Status Panel and more
